
    <div class="flex-1 min-w-0">
        <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:text-3xl dark:text-white">{{$title}}</h2>
        @if(!empty($text))
        <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">{{$text}}</p>
        @endif
    </div>
